{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Ottabase Monorepo Config",
    "description": "Root-level config consumed by monorepo scripts (dev, build, cf:*) to resolve the active app.",
    "type": "object",
    "properties": {
        "defaultApp": {
            "type": "string",
            "description": "Directory name under apps/ to use when no --app flag or OTTABASE_APP env var is set.",
            "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$"
        }
    },
    "additionalProperties": false
}
