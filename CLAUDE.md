# Claude Code entry point

@AGENTS.md

Before working inside any `packages/<pkg>/` or `apps/<app>/` directory, read that directory's
`AGENTS.md` first — it is the token-lean digest of verified imports, canonical usage, and wiring
steps for that package. Deep runbooks live in `docs/agents/` (auth-sessions,
add-package-with-model, dependencies, environment).
