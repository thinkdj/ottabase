// ============================================================
// AUTO-GENERATED PRISMA SCHEMA
// ============================================================
// App: ottabase-template-app
// Provider: d1
// Generated: 2025-12-11T03:08:31.745Z
//
// DO NOT EDIT THIS FILE DIRECTLY
// Edit source schemas and run: pnpm db:generate
// ============================================================
// ============================================================
// Cloudflare D1 Configuration
// ============================================================
// Provider: D1 (SQLite-compatible)
// Adapter: @prisma/adapter-d1
//
// Usage in Cloudflare Worker:
//   import { PrismaClient } from "@prisma/client";
//   import { PrismaD1 } from "@prisma/adapter-d1";
//
//   const adapter = new PrismaD1(env.OBCF_D1);
//   const prisma = new PrismaClient({ adapter });
// ============================================================

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}


// ---- Base: @ottabase/db ----
// ============================================================
// CORE MODELS
// ============================================================
// These models are included by default in all applications.
// When using @ottabase/auth, relations are added via schema concatenation.
// ============================================================

/// User model - Auth.js compatible base fields
/// When @ottabase/auth is enabled, this model is extended with auth relations
model User {
 id String @id @default(cuid())
 name String?
 email String? @unique
 emailVerified DateTime?
 image String?
 createdAt DateTime @default(now())
 updatedAt DateTime @updatedAt
}

/// Post model - Blog posts and content management
model Post {
 id String @id @default(cuid())
 title String
 content String?
 published Boolean @default(false)
 authorId String?
 createdAt DateTime @default(now())
 updatedAt DateTime @updatedAt
}


// ---- App: ottabase-template-app ----
// ============================================================
// OTTABASE TEMPLATE APP - Application-Specific Schema
// ============================================================
//
// This file contains models specific to the template app.
// It will be concatenated with base, core, and feature schemas
// during the db:generate process.
//
// ============================================================

model OttabaseTemplateAppData {
 id String @id @default(cuid())
 userId String @unique
 bio String?
 createdAt DateTime @default(now())
 updatedAt DateTime @updatedAt
}

/// Todo model - Example for D1 demo
/// Demonstrates Prisma ORM with Cloudflare D1
model Todo {
 id Int @id @default(autoincrement())
 title String
 completed Boolean @default(false)
 createdAt DateTime @default(now())

 @@index([createdAt])
}
