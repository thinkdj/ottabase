/**
 * @deprecated – Use types from `@ottabase/brand-engine` directly.
 *
 * This file re-exports BrandEngine types under the original names
 * so that any remaining consumers continue to work.
 */
export type { TokenTypography as ThemeTypography } from '@ottabase/brand-engine';
export type { TokenColors as ThemeColors } from '@ottabase/brand-engine';
export type { TokenShadows as ThemeShadows } from '@ottabase/brand-engine';
export type { TokenMotion as ThemeMotion } from '@ottabase/brand-engine';
export type { TokenCursors as ThemeCursors } from '@ottabase/brand-engine';
export type { BrandTheme as ThemeConfig } from '@ottabase/brand-engine';
