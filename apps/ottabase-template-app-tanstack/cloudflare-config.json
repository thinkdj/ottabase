{
    "$schema": "../../schemas/cloudflare-config.schema.json",
    "deployable": true,
    "appType": "tanstack",
    "workerName": "ottabase-template-app-tanstack",
    "buildCommand": "build",
    "workerBuildCommand": "",
    "outputDirectory": "dist",
    "verifyPaths": ["dist", "cloudflare-worker.ts"],
    "wranglerConfig": "wrangler.jsonc",
    "wranglerEnv": "production",
    "healthCheckPath": "/",
    "requiresSecrets": [],
    "requiresPreviewSecrets": [],
    "$comment": "requiresSecrets/requiresPreviewSecrets are optional — wrangler.jsonc placeholders are auto-detected. Add here only for non-wrangler secrets (e.g. build-time)."
}
