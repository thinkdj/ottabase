// Vitest stubs for packages that may not have dist built in the test environment
export const DEFAULT_LAYOUT = {};
export const resolveLayoutForPath = () => ({});
export const LayoutMetaProvider = ({ children }: any) => children;
export const LayoutSlotsProvider = ({ children }: any) => children;
export const useResolvedLayoutMeta = () => ({});
