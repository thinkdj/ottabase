/**
 * Re-export shim — worker/lib/* files import CloudflareEnv from '../cloudflare-env'.
 * The canonical definition lives one directory up at the app root.
 */
export type { CloudflareEnv } from '../cloudflare-env';
